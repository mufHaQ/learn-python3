#!/usr/bin/env python3

# Index Operator []: Gives access to a sequence's element (str, list, tuple)

name = "dliyaulhaq Mufliansyah"

if name[0].islower():
    name[0] = name[0].upper()

print(name)
